return {
  {
    "skywind3000/asyncrun.vim",
    lazy = false, -- Pode ser true se não precisa carregar imediatamente
    config = function()
      -- Configuração opcional do asyncrun, se necessário
    end
  }
}

